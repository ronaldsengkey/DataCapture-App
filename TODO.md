# TODO

- [x] Fix persistence: repair `src/services/DatabaseService.ts` schema and ensure `fetchActiveItems()` returns stored captures.
- [x] Fix CI/test failure: prevent deprecated `expo-file-system` writes during Jest in `src/services/ExportService.ts`.
- [ ] Smoke test in app: Camera -> Review shows detected sources; URL capture -> Review shows detected sources; File upload -> Review shows detected sources.

